// the spread operator(...) in js used to spreadout the elements of an array or the properties of an object.
// it allows copy,combine,or expand array or object easily.

const arr1 = [1,2,3];
const arr2 = [...arr1];
console.log(arr2);
